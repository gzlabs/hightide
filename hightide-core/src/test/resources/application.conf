dirs {
    src = "src/test/resources/app"
    routes = ${dirs.src}"/routes"
    templates = ${dirs.src}"/templates"
    build = "target"
    gen-src = ${dirs.build}"/generated-sources/main/java"
    static = "static"
}
